# frozen_string_literal: true

# Specify the source for Ruby gems
source "https://rubygems.org"

# Define the Jekyll gem and its version
gem "jekyll", "~> 4.4.1"

# Define the theme for the site
gem "minima", "~> 2.5.2"


# Plugins for Jekyll
group :jekyll_plugins do
  # RSS feed generator
  gem "jekyll-feed", "~> 0.6"
  # Other plugins can be added here as needed
end
